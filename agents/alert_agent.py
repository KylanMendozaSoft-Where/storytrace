# Stub — real implementation in PR-08 (D2)
# Reads:  state['scored_list'], state['job_id']
# Writes: state['alerts_fired'] (list of outlet names where drift_score >= 70)
def run(state: dict) -> dict:
    return state

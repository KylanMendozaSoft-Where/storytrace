# Stub — real implementation in PR-12 (D3)
# Reads:  state['scored_list']
# Writes: state['tree']; also sets art['country'] on each scored_list dict before building
def run(state: dict) -> dict:
    return state

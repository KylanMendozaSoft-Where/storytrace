# Stub — real implementation in PR-10 (D3)
# Reads:  state['articles']
# Writes: mutates art['text'] and art['language'] in-place for non-English articles
def run(state: dict) -> dict:
    return state
